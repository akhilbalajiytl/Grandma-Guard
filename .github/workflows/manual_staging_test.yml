# .github/workflows/manual_staging_test.yml (The Actual, Final, Corrected Version)

name: Manual Staging Test

on:
  workflow_dispatch:

jobs:
  staging-test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      
    env:
      MYSQL_USER: staging_user
      MYSQL_PASSWORD: staging_password
      MYSQL_DATABASE: staging_db
      MYSQL_ROOT_PASSWORD: staging_root_password
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      HF_CACHE_PATH: /home/runner/.cache/huggingface
      DOCKER_IMAGE: ghcr.io/${{ github.repository_owner }}/llm-safety-toolkit

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Cache Hugging Face models
        uses: actions/cache@v4
        with:
          path: ${{ env.HF_CACHE_PATH }}
          key: ${{ runner.os }}-hf-${{ hashFiles('**/requirements.txt') }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
          
      - name: "Build and push Docker image"
        uses: docker/build-push-action@v5
        with:
            context: .
            push: true 
            tags: ${{ env.DOCKER_IMAGE }}:latest
            cache-from: type=registry,ref=${{ env.DOCKER_IMAGE }}:buildcache
            cache-to: type=registry,ref=${{ env.DOCKER_IMAGE }}:buildcache,mode=max

      # Step 1: Start ONLY the background service (the database).
      # We use docker-compose.ci.yml and only specify the 'db' service to start.
      # The webapp service is NOT started as a long-lived process here.
      - name: Start Database Service
        env:
          WEBAPP_IMAGE: ${{ env.DOCKER_IMAGE }}:latest # Still needed for docker compose's internal variable substitution
        run: docker compose -f docker-compose.ci.yml up -d db

      # Step 2: Use 'docker compose run' to create a temporary container
      # just to check if the 'db' service is healthy.
      # We still need --entrypoint "" here because 'sh -c "nc -z db 3306"'
      # is the command we want to run, not the webapp's default entrypoint.
      - name: Wait for Database
        env:
          WEBAPP_IMAGE: ${{ env.DOCKER_IMAGE }}:latest
        run: |
          echo "Waiting for database to be ready..."
          i=0
          while ! docker compose -f docker-compose.ci.yml run --rm --entrypoint "" webapp sh -c "nc -z db 3306"; do
            i=$((i+1))
            if [ $i -ge 24 ]; then
              echo "Database did not become available in time."
              docker compose -f docker-compose.ci.yml logs db
              exit 1
            fi
            echo -n "."
            sleep 5
          done
          echo ""
          echo "Database is ready!"
      
      # Step 3: Use 'docker compose run' again for the main CLI task.
      # This creates another clean, one-off container that connects to the running DB.
      # The '--entrypoint ""' is CRITICAL and VALID for 'run'. It bypasses
      # the web server's startup script and passes the arguments directly
      # and cleanly to Python, resolving the argument mangling.
      - name: Run Scan (CLI Task)
        env:
          WEBAPP_IMAGE: ${{ env.DOCKER_IMAGE }}:latest
        run: |
          set -o pipefail
          docker compose -f docker-compose.ci.yml run --rm \
            --entrypoint "" \
            -e CI=true \
            -e DATABASE_URL="mysql+pymysql://staging_user:staging_password@db/staging_db" \
            -e OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }} \
            webapp python run_cli.py \
              --model-name "Staging Test: GPT-3.5-Turbo" \
              --api-endpoint "https://api.openai.com/v1/chat/completions" \
              --api-key "${{ secrets.OPENAI_API_KEY }}" \
              --openai-model "gpt-3.5-turbo" \
          | tee staging_scan_output.log

      # Step 6: Publish the Job Summary from the log file
      - name: Publish Test Summary
        if: success()
        run: |
          score_line=$(grep 'Score:' staging_scan_output.log | tail -n 1)
          if [ -z "$score_line" ]; then
              score_line="Score: Not found"
          fi
          echo "### 🛡️ LLM Safety Toolkit: Manual Staging Test" >> $GITHUB_STEP_SUMMARY
          echo "**Commit:** ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
          echo "**Result:** ✅ Real Scan Completed" >> $GITHUB_STEP_SUMMARY
          echo "**Final Score:** \`${score_line}\`" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "<details><summary>Click to view full scan log</summary>" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          cat staging_scan_output.log >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "</details>" >> $GITHUB_STEP_SUMMARY

      # Step 7: Display logs only if a step failed
      - name: Display container logs on failure
        # We need WEBAPP_IMAGE here for docker compose logs to resolve the service correctly.
        env:
          WEBAPP_IMAGE: ${{ env.DOCKER_IMAGE }}:latest
        if: failure()
        run: docker compose -f docker-compose.ci.yml logs